import { Encoded } from '../utils/encoder';
/**
 * Returns account address that signed a transaction
 * @param transaction - transaction to get a signer of
 */
export default function getTransactionSignerAddress(transaction: Encoded.Transaction): Encoded.AccountAddress;
