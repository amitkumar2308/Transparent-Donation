import uInt from "./u-int.mjs";
import { AE_AMOUNT_FORMATS, formatAmount } from "../../../utils/amount-formatter.mjs";
export default {
  ...uInt,
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  serializeAettos(value, params) {
    return value !== null && value !== void 0 ? value : '0';
  },
  serialize(value, params, _ref) {
    let {
      denomination = AE_AMOUNT_FORMATS.AETTOS
    } = _ref;
    return uInt.serialize(this.serializeAettos(value != null ? formatAmount(value, {
      denomination
    }) : value, params));
  }
};
//# sourceMappingURL=coin-amount.mjs.map