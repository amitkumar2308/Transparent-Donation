import { encode, Encoding, decode } from "../../../utils/encoder.mjs";
export default function genWrappedField(tag) {
  return {
    serialize(payload, _ref) {
      let {
        buildTx
      } = _ref;
      return decode(buildTx({
        tag,
        payload
      }));
    },
    deserialize(buffer, _ref2) {
      let {
        unpackTx
      } = _ref2;
      return unpackTx(encode(buffer, Encoding.Transaction), tag).payload;
    },
    recursiveType: true
  };
}
//# sourceMappingURL=wrapped.mjs.map